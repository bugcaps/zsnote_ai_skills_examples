# 아키텍처 원칙

1. **단일 책임 원칙 (SRP)**
   - 하나의 클래스/함수는 하나의 기능만 수행해야 합니다.
   - DB 접근 로직과 비즈니스 로직을 분리하세요.

2. **의존성 주입 (DI)**
   - 하드코딩된 의존성을 피하고, 생성자나 함수의 인자로 주입받도록 설계하세요.

3. **에러 핸들링**
   - 단순 `print()`나 `pass`로 예외를 무시하지 마세요.
   - 명시적인 커스텀 예외 클래스를 사용하거나 로깅을 남기세요.
