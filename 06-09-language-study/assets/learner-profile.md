# 학습 프로필 — {{ 목표 언어 }}

> 이 파일은 학습자의 것입니다. 스킬이 읽고 갱신하지만 사람이 직접 고쳐도 되고, 사람이 고친 값은 그대로 둡니다.
> **표의 열을 늘리거나 줄이지 않습니다.** 쓸 값이 없으면 `미정`으로 둡니다. 복사한 뒤 예시 행은 지우고 실제 항목만 남깁니다.
> 이름·소속·연락처·주소는 어느 칸에도 적지 않습니다. 예시의 고유명사는 `[이름]`·`[회사]`로 가려 적습니다.
> 정원과 승격·강등·졸업·삭제 규칙은 SKILL.md `작업 순서` 4단계와 `핵심 규약`을 따릅니다.

## 기본 정보

| 항목 | 값 |
| :--- | :--- |
| 목표 언어 | {{ 한 개만. 배우는 언어가 여럿이면 파일을 따로 만듦 }} |
| 작성 언어(모국어) | {{ }} |
| 수준 | {{ 초급 / 중급 / 고급 — 본인이 말한 값만 적고 스킬이 판정하지 않음 }} |
| 학습 목적 | {{ 업무 / 시험 / 여행 / 일상 대화 중 최대 2개 }} |
| 교정 강도 | {{ 문법만 / 문법+자연스러움 — `미정`이면 `문법만`으로 봄 }} |
| 고치지 말 것 | {{ 사용자가 자기 스타일이라고 지정한 표현. 없으면 `없음` }} |
| 누적 교정 회차 | {{ N }} |
| 마지막 갱신일 | {{ YYYY-MM-DD }} |

## 반복 실수 패턴 (활성)

교정할 때 가장 먼저 보는 목록입니다.

| # | 패턴 이름 | 예시(틀린 표현 → 고친 표현) | 관측 횟수 | 마지막 관측 회차 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | {{ 한 줄 }} | {{ 표현만. 문장 전체나 고유명사를 옮기지 않음 }} | {{ }} | {{ }} |

## 관찰 중

아직 활성으로 올리지 않은 후보입니다. 여기서 빠질 때는 흔적을 남기지 않습니다.

| 패턴 이름 | 예시(틀린 표현 → 고친 표현) | 관측 횟수 | 마지막 관측 회차 |
| :--- | :--- | :--- | :--- |
| {{ }} | {{ }} | {{ }} | {{ }} |

## 졸업

활성에서 빠진 패턴의 이름만 남깁니다. 예시와 관측 횟수는 남기지 않습니다.

| 패턴 이름 | 졸업 회차 |
| :--- | :--- |
| {{ }} | {{ }} |
